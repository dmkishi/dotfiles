# Dedicated config file for shell aliases, which allows DRY alias configs for
# both Bash and Zsh. This file must be referenced by both .bashrc and .zshrc:
#
#   if [ -f ~/.shell_aliases ]; then
#     source ~/.shell_aliases
#   fi




# General
alias ls='ls -FG'
alias ll='ls -lh'
alias la='ls -Alh'
alias ..='cd ..'
alias ...='cd ../..'
alias h='history'
alias less='less -R'  # Always show ANSI-escaped colors

# Git
alias g='git status'
alias gd='git diff'
alias ga='git add'
alias gb='git branch'
alias gc='git commit'
alias gca='git commit --amend'
alias gl='git log'
alias glh='git log | head'
alias tower='gittower'

# Colorize filepath for ag, AKA Silver Searcher
alias ag='ag --color-path=31'