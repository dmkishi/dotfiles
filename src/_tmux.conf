################################################################################
# General
################################################################################
# Enable 256 color support and italics.
set -g default-terminal 'tmux-256color'

# Remap prefix from `C-b` to `C-a` while allowing you to go to beginning of line
# by typing `C-a C-a`.
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Set title bar
#   #H  Hostname of local host
#   #I  Current window index
#   #P  Current pane index
#   #S  Session name
#   #T  Current window title
#   #W  Current window name
#   #   A literal ‘#’
# See <https://superuser.com/questions/430650/let-tmux-set-urxvt-window-title-according-to-session-name>
set -g set-titles on
# set -g set-titles-string '#T “#S”'
set -g set-titles-string '#S'

# Start numbering windows and panes at 1
set  -g base-index 1       # Windows
setw -g pane-base-index 1  # Panes

# Set scroll-back buffer to 10,000 lines
set -g history-limit 10000

# Clear screen
bind -n C-k send-keys C-l

# Reload config shortcut
bind r source-file ~/.tmux.conf \; display-message "Config reloaded"

# Split panes
bind-key | split-window -h
bind-key - split-window

# Allows for faster key repetition
set -s escape-time 0

# Rather than constraining window size to the maximum size of any client
# connected to the *session*, constrain window size to the maximum size of any
# client connected to *that window*. Much more reasonable.
#setw -g aggressive-resize on

# Activity monitoring
#setw -g monitor-activity on
#set -g visual-activity on




################################################################################
# Status Bar
################################################################################
set-option -g status-position     top
set        -g status-interval     5       # Redraw interval in seconds
set        -g status-justify      left
set        -g status-left         ''
set        -g status-right        '"#S"'  # Session name
set        -g status-left-length  40
set        -g status-right-length 20

# Status and Window Title Colors and Format
#   #F Window Flag
#        #: Window activity
#        !: Window bell
#        ~: Window silence
#        *: Current window
#        -: Last window
#        Z: Window zoomed
#         : Empty, no flags at all
#   #I Window Index
#   #W Window Name

set -g window-status-format         '  #I#{?window_zoomed_flag, ZOOMED,} #W  '
set -g window-status-current-format '  #I#{?window_zoomed_flag, ZOOMED,} #W  '
set -g window-status-current-style bg=colour89,fg=white
set -g pane-active-border-style    fg=colour226               #Yellow
set -g pane-border-style           fg=black                   #Solarized light-blue
set -g message-style               bg=colour235,fg=colour166  #Base02 / Orange

# Pane Number Display
set-option -g display-panes-active-colour         colour33   #Blue
set-option -g display-panes-colour                colour166  #Orange




################################################################################
# Motion
################################################################################
# Pane traversal. This moves away from the home-row but should provide smoother
# navigation due to only a single key-combination. To compensate, tab-cycling
# is executable single-handedly.
bind -n C-Left  select-pane -L
bind -n C-Down  select-pane -D
bind -n C-Up    select-pane -U
bind -n C-Right select-pane -R
# bind-key Tab select-pane -t :.+

set-window-option -g mode-keys vi

# Mouse-mode defaults to on. Toggle via PREFIX + m/M. Reasons to disable: (i)
# mouse-control panes in vim, (ii) select text, and ???
set -g mouse on
bind m set -g mouse off \; display 'Mouse: OFF'
bind M set -g mouse on  \; display 'Mouse: ON'

# Enable mouse scroll <https://github.com/tmux/tmux/issues/145#issuecomment-150736967>
#bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'copy-mode -e'"




################################################################################
# Tmux Plugin Manager
################################################################################
# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'nhdaly/tmux-better-mouse-mode'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'
